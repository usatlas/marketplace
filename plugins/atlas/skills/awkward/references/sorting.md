# Sorting

Use `ak.sort` to sort within lists.

```python
sorted_default = ak.sort(array)          # sorts along last axis (axis=-1)
sorted_desc = ak.sort(array, ascending=False)
sorted_axis1 = ak.sort(array, axis=1)
```
